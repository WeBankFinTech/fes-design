import version from './version';

import { install } from './preset';

export * from './components';
export * from './locales';

export { version, install };

export default {
    version,
    install,
};
